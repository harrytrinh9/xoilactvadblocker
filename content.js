$(document).ready(function(){
    // var topBanner = document.getElementsByClassName('container mt-1 d-none d-lg-block');

    // console.log(topBanner);
    // topBanner.remove();
    jQuery('#overlay').prop('style', 'display: none;');
    var bottom = document.getElementById('quangcaopc');
    $(bottom).remove();
})


function removeQC(){
    // jQuery('#overlay').prop('style', 'display: none;');
    // jQuery('#quangcaopc').remove();
    // var topBanner = $('.container .mt-1 .d-none .d-lg-block');
    // var topBanner = document.getElementsByClassName('container mt-1 d-none d-lg-block');

    // console.log(topBanner);
    // topBanner.remove();
}
